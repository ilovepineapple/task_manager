<% content_for(:title, 'Edit task') %>

<h1>Edit Task</h1>

<%= form_for(@task) do |f| %>
  <%= render(partial: 'form', locals: { f: f }) %>

  <div>
    <%= f.submit("Edit Task") %>
  </div>
<% end %>
